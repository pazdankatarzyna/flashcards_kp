[project]
name = "flashcards-kp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-json-logger>=4.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.0",
    "ty>=0.0.17",
]
